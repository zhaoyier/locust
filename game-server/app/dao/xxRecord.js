var utils = require('../util/utils');

var xxRecord = module.exports;

xxRecord.recordHandCard = function(userId, card, pattern, teamId){
	//utils.invokeCallback(callback, null, {userId: 100, balance: 200});
}

xxRecord.recordBet = function(userId, amount, teamId){
	//utils.invokeCallback(callback, null, {userId: 100, balance: 200});
}

//